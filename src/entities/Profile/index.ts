import { ProfileSchema, Profile } from './model/type/profile';
import { profileReducer, profileActions } from './model/slice/profileSlice';

export {
    ProfileSchema, Profile, profileReducer, profileActions,
};
